# Ensure shell scripts always use LF line endings (critical for Docker on Linux)
*.sh text eol=lf

# Default: normalize line endings to LF in repo, use OS native on checkout
* text=auto

# Force LF for these file types regardless of OS
*.sh text eol=lf
Dockerfile text eol=lf
*.dockerfile text eol=lf
.env* text eol=lf
